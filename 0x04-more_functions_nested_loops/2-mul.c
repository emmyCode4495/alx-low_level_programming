#include "main.h"

/**
 *mul - function to multiply 2 numbers
 *@x: The first variable to be returned
 *@y: the scond variable to be returned
 *Return: the final result to be returned after compilation
 */
int mul(int x, int y)
{
	return (x * y);
}
