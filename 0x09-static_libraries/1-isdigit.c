#include "main.h"

/**
 * _isdigit - fnction that checks for a digit number
 * @c: the variable to be checked
 * Return: 1 if c is a digit, 0 otherwise
 */
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}

