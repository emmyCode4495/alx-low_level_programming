#include "main.h"


/**
 * _abs - the function that prints absolute value
 * @n: the inout integer to be checked
 * Return: the absolute value of int
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}

